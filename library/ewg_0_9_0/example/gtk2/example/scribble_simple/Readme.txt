This example demonstrates how to use the generated GTK wrapper.
It creates a window, in which the user can draw with the mouse.
It uses agent as a signal mechanism and thus currently only works
on ISE Eiffel and SmartEiffel
